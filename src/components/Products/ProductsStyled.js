import styled from 'styled-components'
import { Subtitle } from '../GlobalStyled/GlobalComponents'

export const ProductsStyled = styled.section`
  display: flex;
  flex-direction: column;
  gap: 3.5rem;
`

export const ProductsTitleStyled = styled(Subtitle)`
  text-align: center;
`
