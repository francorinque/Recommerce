import styled from 'styled-components'

export const InputSelectStyled = styled.select`
  border: none;
  padding: 0.5rem;
  border-radius: var(--radius);
  background: var(--bg-2);
  color: var(--text);
`

export const InputSelectOptionStyled = styled.option``
